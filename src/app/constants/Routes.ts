const defRoute = "/pages";
export const Routes = {
    LOGIN: `${defRoute}/login`,
    REGISTER: `${defRoute}/register`,
    HOME: `${defRoute}/home`,
    PROFILE: `${defRoute}/profile`,
    NOT_FOUND: `${defRoute}/notFound`
}
